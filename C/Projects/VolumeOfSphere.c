#include<stdio.h>
int main(){
    float radius;
    radius = 3.5;
    float pi = 3.1418;

    float volume = 4 * pi * radius * radius * radius;

    printf("The Volume is : %f", volume);
    
    return 0;
}